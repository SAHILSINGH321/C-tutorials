#include<stdio.h>
// enter a number to find whether it is lying between 5 and 10. (logical operators )
/*int main () {
int n;
printf("Enter number : ");
scanf("%d,&n");
if (n>=5 && n<=10){
    printf("It is lying between 5 and 10.");
}
else {
    printf("It is not lying between 5 and 10.");
}
return 0;
}
*/

// Take positive integer input and tell if it is a three digit number or not.

/*int main () {
int n;
printf("Enter number : ");
scanf("%d,&n");
// n<=99 && n>=1000 ---> It is not a three digit number.
// n>=100 && n<=999 ---> It is  a three digit number.
if ( n>=100 && n<=999 ) {
   printf("It is a three digit number. ");
}
else {
    printf("It is not a three digit number.");
}
    return 0;
}
*/
//  Take positive integer as input and tell if it is divisible by 5 or 3;
 
 /* int main () {
int n;
printf("Enter number : ");
scanf("%d,&n");
if (n%5==0 || n%3==0){
    printf("It is divisible by 5 or 3.");
}
else {
    printf("it is not divisible by 5 or 3");
}
return 0;
 }
 */

// Take 3 positive integers as input and print greatest among them.
/* int main (){
    int a,b,c;
    printf("Enter a : ");
    scanf("%d",&a);
     printf("Enter b : ");
    scanf("%d",&b);
     printf("Enter c : ");
    scanf("%d",&c);
    if (a>b && a>c){
        printf("Greatest of them : %d",a);
    }
   else if (b>a && b>c){
        printf("Greatest of them : %d",b);
    }
    else if (c>a && c>b){
        printf("Greatest of them : %d",c);
    }
    else {
        printf("No number is greatest among them.");
    }
    return 0;
}*/

// Take 3 positive integers as input hence find whether they are either sides of  tringle. 
/*int main (){
    int a,b,c;
    printf("Enter a : ");
    scanf("%d",&a);
     printf("Enter b : ");
    scanf("%d",&b);
 printf("Enter c : ");
    scanf("%d",&c);
 if ((a+b)>c && (b+c)>a && (a+c)>b){
    printf("They are sides of triangle.");
 }
 else {
     printf("They are  not sides of triangle.");
 }
return 0;
}
*/
